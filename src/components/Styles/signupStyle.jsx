import { makeStyles } from "@mui/styles";


const useStyles = makeStyles(theme => ({
    sideBar: {
        backgroundColor: '#272964',
        height: '100vh',
        position: 'fixed',
        width: '33.333333%',
        color: '#fff'
    }
}))
export default useStyles